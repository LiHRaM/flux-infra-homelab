---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: lidarr
spec:
  selector:
    matchLabels:
      app: lidarr
  template:
    spec:
      containers:
      - name: lidarr
        volumeMounts:
        - mountPath: /media
          name: media
        - mountPath: /config
          name: lidarr-config
      volumes:
      - name: media
        nfs:
          path: /media/
          server: 192.168.1.188
      - name: lidarr-config
        persistentVolumeClaim:
          claimName: lidarr-config