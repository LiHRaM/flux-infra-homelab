apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: music-assistant
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: music-assistant
  policy:
    semver:
      range: '>=2.9.0 <3.0.0'
