apiVersion: v1
kind: Service
metadata:
  name: tei
  namespace: embed
spec:
  selector:
    app: tei
  ports:
    - port: 80
      name: http
      protocol: TCP
      targetPort: 80
  type: ClusterIP
